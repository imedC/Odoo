<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="fbo.list">
      <field name="name">FBO list</field>
      <field name="model">fbo.fbo</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="fbo.action_window">
      <field name="name">FBO window</field>
      <field name="res_model">fbo.fbo</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="fbo.action_server">
      <field name="name">FBO server</field>
      <field name="model_id" ref="model_fbo_fbo"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="FBO" id="fbo.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="fbo.menu_1" parent="fbo.menu_root"/>
    <menuitem name="Menu 2" id="fbo.menu_2" parent="fbo.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="fbo.menu_1_list" parent="fbo.menu_1"
              action="fbo.action_window"/>
    <menuitem name="Server to list" id="fbo" parent="fbo.menu_2"
              action="fbo.action_server"/>
    -->
  </data>
</odoo>